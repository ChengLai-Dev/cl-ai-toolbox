# 示例

## 信息收集对话

**用户**: 我想学习 C++ 多态。

**AI**:
好的，我来为你设计多态的学习课程。请先回答几个问题：

1. 你想学习什么主题/学科？（已确认：C++ 多态）
2. 你当前的 C++ 水平是？（完全新手 / 有基础概念 / 有一定实践经验 / 熟练）
3. 你的学习目标是什么？（了解 / 掌握 / 精通）
4. 你希望的学习周期是多久？（几天 / 几周 / 几个月）
5. 你每天大概能投入多少时间？
6. 你更偏好理论先行还是实践驱动？
7. 是否有需要用到的具体项目场景或工具版本？
8. 你希望以什么形式产出课程？（聊天交互式 / 完整课程文档 / 混合式）

**用户**: 我有 C++ 基础概念，目标是掌握使用，一周时间，每天 1 小时，理论先行再加实践，场景是 CLEngine2D 引擎项目，C++23。产出成文档吧，我自己安排时间学。

**AI**: 好的，完整课程文档模式。我现在一次性生成《C++ 多态》完整课程包，写入当前目录的 `C++多态/` 文件夹。

## 学习路径输出示例（聊天交互式）

**AI**:

学习路径如下：

```
阶段 1（第 1-2 天）：从虚函数到运行时多态
├─ 章节 1：虚函数机制（vptr/vtable）
├─ 章节 2：override 与 final
└─ 章节 3：纯虚函数与抽象类

阶段 2（第 3-4 天）：编译时多态
├─ 章节 4：函数重载与运算符重载
├─ 章节 5：模板与模板特化
└─ 章节 6：CRTP 简介

阶段 3（第 5-6 天）：多态在实际设计中的应用
├─ 章节 7：策略模式
└─ 章节 8：工厂模式

阶段 4（第 7 天）：综合项目 + 复习
└─ 章节 9：小项目 + 回顾
```

今天我们先学**章节 1**，学完我们再进入下一章节。

## 完整章节讲义示例（章节 1：虚函数机制）

### 章节 1：虚函数机制

**本章目标**：理解虚函数在底层如何实现运行时多态，能写出正确的虚函数继承代码。

**核心概念**：
- vtable（虚函数表）——每个类一张表，存该类的虚函数地址
- vptr（虚指针）——每个对象一个指针，指向该类对应的 vtable
- 动态绑定——运行时通过 vptr → vtable → 函数地址确定调用哪个函数
- 析构函数应为虚函数——确保通过基类指针 delete 时正确调用子类析构

**讲解**：

**1. vtable 和 vptr 是什么**

C++ 标准没有规定运行时多态必须怎么实现，但所有主流编译器都使用同一方案：vtable（虚函数表）+ vptr（虚指针）。

- **vtable**：对于每个含有虚函数（或继承了虚函数）的类，编译器会为该类生成一张**静态的表**，按声明顺序存储该类的所有虚函数的函数指针。这张表**编译时就确定了**，存在程序的**只读数据段（.rodata）**，该类的所有对象共用同一张表。
- **vptr**：每个含有虚函数的对象中，编译器会在对象的**开头（前 8 字节，64 位下）**插入一个隐藏指针——vptr。它指向**该类对应的 vtable**。
- **初始化时机**：vptr 在**构造函数初始化列表阶段**被设置为该类的 vtable 地址。多层继承时，每层构造函数都会重置 vptr 为自己的 vtable——所以构造函数中调虚函数不会发生动态绑定。

**2. 动态绑定如何工作**

当通过基类指针调用虚函数时，编译器生成间接调用代码：

```cpp
Animal* p = &dog;
p->speak();
// 编译器生成的伪代码：
// vtable = p->vptr      // 读取 vptr，指向 Dog 的 vtable
// func = vtable[0]      // speak 在 vtable 中偏移为 0
// func(p)               // 间接调用 Dog::speak()
```

调用结果取决于 `p` **实际指向的对象类型**，而非 `p` 的静态类型（`Animal*`），所以叫**动态绑定**（运行时才决定）。

```cpp
class Animal {
public:
    virtual void speak() { std::cout << "Animal speaks\n"; }
    virtual ~Animal() = default;
};

class Dog : public Animal {
public:
    void speak() override { std::cout << "Woof!\n"; }
};

void makeSpeak(Animal* animal) {
    animal->speak();
}
```

**本章小结**：
- vtable 编译期确定、存于 .rodata、所有对象共用；vptr 构造时赋值、每个对象一个
- 动态绑定 = 运行时经 vptr → vtable → 函数地址间接调用
- 基类析构必须声明为 virtual，否则 delete 基类指针跳过派生类析构，导致资源泄漏

**本章自测**：完成测验：见 `../quizzes/01-虚函数机制.md`。

## 交互推进示例（费曼总结 + 间隔重复）

**AI**: 恭喜你完成章节 1。现在请**用自己的话**总结一下：vptr、vtable 和动态绑定分别是什么？它们之间是什么关系？

**用户**: vptr 是对象里的一个指针，vtable 是类的虚函数表。调用虚函数时通过 vptr 找到表，再找到函数。大概是这样。

**AI**（掌握度：良）: 关系说对了。补充一个细节：vtable 是编译时就确定、存在 .rodata 的静态表，所有对象共用同一张。vptr 才是在构造时被赋值的。下次再遇到"初始化时机"类问题留意一下。

下面回顾一个之前章节的问题：`override` 和重载（overload）有什么区别？

**用户**: override 是重写父类函数，overload 是同一个类里同名不同参数。

**AI**: 正确，两个关键点都答对了。接下来进入章节 2：override 与 final。

## 文档模式交付示例

**AI**: 已完成《C++ 多态》完整课程包，已写入 `C++多态/`，包含：

- **COURSE.md**：4 个阶段、9 个章节、时间分配
- **chapters/**：9 个章节讲义，每章含学习目标、核心概念、讲解、小结
- **quizzes/**：9 个章节测验，混合题型，答案附在底部
- **PROGRESS.md**：进度表预置为"未开始"

你可以按文档自主安排学习节奏。如果学完后想回到对话式检查理解（费曼总结、抽查复习），随时告诉我，我会按聊天交互式模式继续。
